# repoindex-demo-analyzer

Example third-party analyzer plugin for `repoindex`.

## Install

```bash
source /path/to/repoindex/.venv/bin/activate
pip install -e /path/to/repoindex/examples/plugins/repoindex_demo_analyzer
```

## Verify

```bash
repoindex plugins
```

Expected output includes a loaded analyzer named `demo`.
