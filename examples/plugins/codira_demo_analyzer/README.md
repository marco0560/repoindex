# codira-demo-analyzer

Example third-party analyzer plugin for `codira`.

## Install

```bash
source /path/to/codira/.venv/bin/activate
pip install -e /path/to/codira/examples/plugins/codira_demo_analyzer
```

## Verify

```bash
codira plugins
```

Expected output includes a loaded analyzer named `demo`.
